module.exports = (bot) => { //im just gonna use a snipe catcher instead btw, for sto p deleting why
    console.log(`Signed in as ${bot.user.tag}. and ready on ${bot.guilds.cache.size} servers.`);

};